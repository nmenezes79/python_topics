# Store the sentence in a string
sentence = "I will complete my homework"

# Create a list with the sentence repeated 100 times
punishment_list = [sentence] * 100

# Use a loop to display each sentence
for line in punishment_list:
	print(line,end='\t')