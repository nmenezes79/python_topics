# Step 1: getArea.py (The module)
# Create a Python module getArea.py that defines a getArea() function for different shapes: square, rectangle, circle, and triangle.

# Step 2: football_field_area.py (Main script)
# Create another script football_field_area.py that imports the getArea module and calculates the area of a football field.

# When you run football_field_area.py, the output will be:
# The area of the football field is 6825 square meters.
